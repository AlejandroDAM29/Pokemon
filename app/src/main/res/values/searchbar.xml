<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="searchbar" parent="Widget.AppCompat.SearchView" >
        <!-- Text Size -->
        <item name="android:textSize">20sp</item>
        <!-- Query Text Color -->
        <item name="android:textColorPrimary">@color/mainRedColor</item>
        <!-- Hint Color -->
        <item name="android:textColorHint">@color/mainRedColor</item>
        <!-- Icon Color -->
        <item name="android:tint">@color/mainRedColor</item>
        <item name="android:background">@color/white</item>
    </style>
</resources>